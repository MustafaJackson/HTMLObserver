<Page
    x:Class="HTMLObserver.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HTMLObserver"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="27" Padding="10">I'm Watching you!</TextBlock>
        <StackPanel Grid.Row="1" Padding="10">
            <TextBlock>
                Insert the URL below to be scanned regurally for new edits. 
            </TextBlock>
            <TextBox x:Name="URLTxtBox" Margin="0,10,0,0" Width="300" HorizontalAlignment="Left"/>
            <Button x:Name="SubmitBtn" Click="SubmitBtn_Click" Content="Submit" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
